function count(start) {
    start = start + 1;
    for (let i = start + 1; i < (start + 10); i++) {
        return (start + " then " + i);
    }
}


function countUp(start) {
    start = start + 1;
    for (let i = start + 1; i < (start + 10); i++) {
        console.log (start + " then " + i)
    }
    
}